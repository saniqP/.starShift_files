#!/usr/bin/env python3
from starShift import star_clear
import argparse

parser = argparse.ArgumentParser()
parser.add_argument('-path', required=True, help="Path to .starShift file")
args = parser.parse_args()

result = star_clear(args.path)
print(result)
